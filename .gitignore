# Examples 1 and 2 show that only source/config files were changed, so no .gitignore content is needed

# Source files and documentation changes don't require gitignore entries
# Only build artifacts, dependencies, and temporary files should be ignored